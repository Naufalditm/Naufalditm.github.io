<?php

// $koneksi = mysqli_connect("localhost", "root", "", "watering_plant");

// if ($koneksi->connect_error) {
//     die("Connection failed: " . $koneksi->connect_error);
//   }

  
// $id         = $_REQUEST['id'] ?? 0;
// $siramo     = $_POST['siramo'] ?? "";
// $tanahk     = $_POST['tanahk'] ?? "";
// $normal     = $_POST['normal'] ?? "";


// Simpan data ke DB
// $sql        = mysqli_query($koneksi, "UPDATE threshold SET siramo='$siramo', tanahk='$tanahk', normal='$normal' WHERE id='$id'");
// if($sql) {
//     echo "Berhasil dikirim";
// } else {
//     echo "Gagal terkirim";
// }

// ?>